<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.database.diarymanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FogetPasswordActivity"
            android:exported="false" />
        <activity android:name=".ReadPage" />
        <activity
            android:name=".AddPagePopUp"
            android:theme="@style/AppTheme.PopMe" />
        <activity android:name=".SecretPage" />
        <activity android:name=".EditingPage" />
        <activity android:name=".AddingPage" />
        <activity android:name=".TodaySNote" />
        <activity android:name=".DiaryIndex" />
        <activity android:name=".OpenDiary" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SignUp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>